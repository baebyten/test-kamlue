import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: '"Kanit", sans-serif', // ตั้งฟอนต์ Kanit ให้เป็นฟอนต์หลัก
  },
});

export default theme;
